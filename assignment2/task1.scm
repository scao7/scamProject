;for-loop
(define (range start end n)
	
	(range start end n)
)

(define (for-loop range function)
	
	
	(range) 
)

(define (main)

(define env this)
    (for-loop (range 0 10 1) (lambda (i) (inspect i)))
)